# Ameo Web

Minimal Next.js and TypeScript web client for the Ameo project management tool.

## Development

Install dependencies:

```sh
npm install
```

Run the local web app:

```sh
npm run dev
```

Build for production:

```sh
npm run build
```

This scaffold is UI-only. Backend API routes and persistence are intentionally
out of scope for this first web shell.
